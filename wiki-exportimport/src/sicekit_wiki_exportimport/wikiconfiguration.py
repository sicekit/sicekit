
class WikiConfiguration(object):
	def __init__(self, username, password, domain, url, cookie_filename):
		self.username = username
		self.password = password
		self.domain = domain
		self.url = url
		self.cookie_filename = cookie_filename
	
